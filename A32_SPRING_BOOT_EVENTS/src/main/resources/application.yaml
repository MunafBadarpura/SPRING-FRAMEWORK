spring:
  application:
    name: A32_SPRING_BOOT_EVENTS
    version: 0.0.1-SNAPSHOT
  datasource:
    url: jdbc:postgresql://localhost:5432/shippingDB
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true