spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/MunafBadarpura/ecommerece-config-server-docker
          username: munafbadarpura
          password: ${GITHUB_TOKEN}
          default-label: main


server:
  port: 8888


